# 디자인 가이드 — SAP Private Cloud 운영 학습 포털

기준일: **2026-09-02**

이 문서는 `html/` 사이트의 색상·타이포·레이아웃 규정이다. 구현은 `assets/css/site.css` 하나에 CSS 변수로 들어가 있으므로, 값을 바꾸려면 이 문서와 그 파일만 고치면 된다.

---

## 0. 콘텐츠 원칙

- HTML에는 회사의 프로젝트 현황, 대상 릴리스, 전환 물량, 계정 수, 내부 시스템 구성, 적용 범위, 의사결정 내용을 쓰지 않는다.
- 회사 로고와 브랜드 색상은 쓸 수 있지만 프로젝트 소개나 현황 설명과 연결하지 않는다.
- 운영자가 `learning.sap.com`과 SAP Learning Hub의 공식 교육을 찾고 활용하는 데 필요한 정보만 제공한다.
- 과정명·코드·시간·언어·실습 여부·공식 URL은 유지하고, 내부 맥락은 삭제하거나 일반적인 교육 안내로 바꾼다.

---

## 1. 색상 근거

두 곳에서 가져왔다. 추측한 값은 없다.

| 출처 | 확인 방법 | 역할 |
|---|---|---|
| **SK그룹 CI** — [sk.co.kr/ko/about/ci.jsp](https://www.sk.co.kr/ko/about/ci.jsp) | 공식 CI 페이지의 컬러 시스템 항목을 직접 확인 | 브랜드 기준. 주요·보조 색상 전부 |
| **SAP Learning** — learning.sap.com | 사이트에서 실제로 쓰이는 토큰 `#0040B0` 확인 | 콘텐츠 출처의 톤. 링크·정보 계열 |

SAP는 공식 브랜드 가이드 페이지가 로그인/차단이라 브랜드 팔레트 원문을 읽지 못했다. 그래서 **SAP 색은 실제 관측한 `#0040B0` 하나만** 쓰고, 나머지는 SK CI로 채웠다. 확인 못 한 SAP 색을 지어내지 않았다.

---

## 2. SK CI 팔레트 (원문 그대로)

### 주요 색상

| 이름 | HEX | RGB | PANTONE |
|---|---|---|---|
| **SK Red** | `#EA002C` | 234 0 44 | 186C |
| **SK Orange** | `#F47725` | 255 122 0 | 158C |

> CI 페이지의 SK Orange는 HEX(`#F47725`)와 RGB(255 122 0 = `#FF7A00`)가 서로 다르게 표기돼 있다. 웹 구현이므로 **HEX 표기값 `#F47725`를 따랐다.**

### 보조 색상 8종

| 이름 | HEX | 이 사이트에서의 쓰임 |
|---|---|---|
| SK Yellow | `#FBBC05` | PP(생산) 모듈 |
| SK Green | `#B3CF0A` | PM(설비보전) 모듈 |
| SK Teal | `#009A93` | EWM(창고) 모듈 |
| SK Blue | `#0072C6` | MM(자재/구매) 모듈, 링크 |
| SK Navy | `#0E306D` | FI(재무회계) 모듈, 본문 제목, 헤더 |
| SK Purple | `#68127A` | CO(관리회계) 모듈 |
| SK White | `#FFFFFF` | 바탕 |
| SK Black | `#000000` | — (본문은 순검정 대신 `#1A1D21` 사용) |

### 특수 색상

| 이름 | HEX | 쓰임 |
|---|---|---|
| SK Gold | `#866D4B` | FI-AA(자산회계) — FI 하위임을 색으로 암시 |
| SK Light Gold | `#8A7E70` | HR(인사) |
| SK Silver | `#8A8D8F` | TR(자금), 중립 텍스트 |

---

## 3. 적용 원칙

### 3.1 SK Red는 CTA와 강조에만

SK Red는 SK를 대표하는 색이지만 넓은 면적에 깔면 읽기 부담이 크다. **버튼·활성 표시·중요 배지에만** 쓴다. 본문 배경이나 큰 블록에 쓰지 않는다.

기본 조합은 **SK Navy(구조) + SK Blue(링크) + SK Red(행동)** 이다.

```
--brand-primary   : #EA002C   SK Red      주 CTA, 활성 상태
--brand-secondary : #F47725   SK Orange   보조 강조, 하이라이트
--brand-navy      : #0E306D   SK Navy     헤더, 제목, 다크 섹션
--brand-blue      : #0072C6   SK Blue     링크, 정보
--sap-blue        : #0040B0   SAP         SAP 출처 표시, 외부 링크
```

### 3.2 모듈별 색상 코딩이 이 사이트의 핵심 기능

담당자가 자기 영역을 **읽지 않고 색으로** 찾게 하는 것이 목적이다. 모듈마다 SK 팔레트에서 하나씩 고정 배정했고, 이 배정은 바뀌지 않는다.

| 모듈 | 색 | 변수 |
|---|---|---|
| MM 자재/구매 | SK Blue `#0072C6` | `--m-mm` |
| SD 영업 | SK Orange `#F47725` | `--m-sd` |
| PP 생산 | SK Yellow `#FBBC05` | `--m-pp` |
| PM 설비보전 | SK Green `#B3CF0A` | `--m-pm` |
| FI 재무회계 | SK Navy `#0E306D` | `--m-fi` |
| FI-AA 자산회계 | SK Gold `#866D4B` | `--m-aa` |
| CO 관리회계 | SK Purple `#68127A` | `--m-co` |
| EWM 창고 | SK Teal `#009A93` | `--m-ewm` |
| TM 운송 | SK Red `#EA002C` | `--m-tm` |
| HR 인사 | SK Light Gold `#8A7E70` | `--m-hr` |
| TR 자금 | SK Silver `#8A8D8F` | `--m-tr` |
| EC-CS 연결결산 | SK Navy 60% | `--m-ec` |

노랑·연두는 흰 바탕에서 텍스트 대비가 부족하다. **텍스트에는 어두운 변형(`--m-pp-ink`, `--m-pm-ink`)을 쓰고, 원색은 면·배지·아이콘에만** 쓴다.

### 3.3 자료 충실도를 시각으로 구분

카드를 세 등급으로 나눠 담당자가 기대치를 즉시 알게 한다. 이것도 정보다.

| 등급 | 대상 | 시각 처리 |
|---|---|---|
| **전용 journey 있음** | MM SD PP PM FI CO EWM TM | 컬러 상단 밴드 + 진한 카드 |
| **과정 단위만** | FI-AA HR TR EC-CS | 얇은 컬러 선 + 흐린 카드 + `과정 단위` 배지 |
| **확인된 과정 없음** | Central Finance | 회색 점선 테두리 + `확인된 과정 없음` 배지 |

### 3.4 한국어 표기

한국어 여부는 학습 순서를 정하는 1급 정보다. **제외 기준이 아니다.** 배지로만 표시하고 경고색(빨강)을 쓰지 않는다.

| 배지 | 의미 | 색 |
|---|---|---|
| `한국어` | learning.sap.com에서 바로 열림 | SK Teal 배경 |
| `한국어 · LHub` | Learning Hub 로그인 후 열림 | SK Blue 배경 |
| `영어` | 영어(일부 일본어)만 | 중립 회색 |
| `미확인` | 확인하지 않음 | 흐린 회색 테두리만 |

---

## 4. 타이포그래피

한글이 주 언어이므로 한글 가독성을 먼저 본다. 웹폰트를 외부에서 불러오지 않고 **시스템 폰트 스택**을 쓴다. 오프라인·사내망에서도 깨지지 않는다.

```
--font-sans: "Pretendard", "Pretendard Variable", -apple-system, BlinkMacSystemFont,
             "Segoe UI", "Noto Sans KR", "Malgun Gothic", "Apple SD Gothic Neo",
             Roboto, sans-serif;
--font-mono: "SFMono-Regular", Consolas, "D2Coding", monospace;
```

과정 코드(`S4D437`, `LSC00991`)는 **반드시 mono**로 쓴다. 숫자·영문 코드를 본문과 구분해야 스캔이 빨라진다.

| 용도 | 크기 | 굵기 | 자간 |
|---|---:|---:|---|
| Hero 제목 | 44–56px (clamp) | 800 | -0.02em |
| 섹션 제목 | 30–36px | 750 | -0.015em |
| 카드 제목 | 19px | 700 | -0.01em |
| 본문 | 16px | 400 | 0 |
| 표·보조 | 14px | 400 | 0 |
| 배지·라벨 | 12px | 600 | 0.02em |

행간은 본문 `1.75`, 제목 `1.25`. 한글은 영문보다 행간을 넉넉히 줘야 답답하지 않다.

---

## 5. 레이아웃

첨부한 참조 사이트 두 개에서 가져온 것과 버린 것을 명시한다.

### 가져온 것

- **상단 고정 내비** + 우측 CTA 버튼 (두 사이트 공통)
- **Hero 좌측 텍스트 / 우측 이미지** 2단 (Daily Travel)
- **흰 카드 feature strip** — hero 아래 겹쳐 올리는 4칸 요약 (Lingua)
- **카드마다 다른 강조색을 쓴 그리드** (Lingua의 Popular Languages) — 이 사이트 모듈 카드의 원형
- **다크 CTA 배너** (Lingua)
- **다단 다크 푸터** (두 사이트 공통)

### 버린 것

- 장식용 blob·손그림 화살표·이모지 (Lingua) — 사내 업무 포털에 맞지 않는다
- 기울어진 사진 카드, 사진 위 텍스트 (Daily Travel) — 정보 밀도가 떨어진다
- 가격·별점 카드 — 해당 정보가 없다

### 격자와 여백

```
--max-w      : 1180px      본문 최대 너비
--gutter     : 24px        모바일 좌우 여백
--sec-y      : 88px        섹션 상하 여백 (모바일 56px)
--card-gap   : 20px
--radius     : 16px        카드
--radius-sm  : 10px        배지·버튼
```

모듈 카드 그리드: 데스크톱 4열 → 소형 데스크톱 3열 → 태블릿 2열 → 모바일 1열.

### 답답하지 않게 하는 규칙

사용자 요구가 "너무 답답하지 않게"였다. 구체적으로:

1. 섹션 사이 여백을 **88px** 이상 준다. 표가 연속되지 않게 한다.
2. 표는 한 화면에 **12행을 넘기지 않는다.** 넘으면 나눈다.
3. 카드 안 텍스트는 **3줄 이내.** 상세는 링크로 보낸다.
4. 본문 폭 **72자 이내**로 제한한다.
5. 색은 한 화면에 **모듈색 1개 + 중립**만. 여러 모듈색을 한 화면에 섞는 곳은 index의 카드 그리드뿐이다.

---

## 6. 페이지 종류

| 종류 | 성격 | 밀도 |
|---|---|---|
| `index.html` | 정문. 자기 영역 찾기 | 시각적, 여유 있게 |
| 모듈 상세 (`modules/*.html`) | 그 모듈만 본다 | 정돈된 표 중심 |
| 주제 페이지 (`custom-reports`, `conversion`, `access`, `start`) | 공식 학습 주제 안내 | 정돈된 문서형 |

**index만 시각적이고 나머지는 조용하다.** 콘텐츠 페이지에 hero·큰 이미지·컬러 블록을 넣지 않는다. 상단에 모듈색 얇은 띠 하나로 위치만 알려준다.

---

## 7. 이미지

히어로 이미지는 운영자가 모듈·공식 과정·실습 자료를 찾는 흐름만 표현한다. 특정 프로젝트, 릴리스, 수량, 내부 기술 구성을 넣지 않는다.

필요한 이미지 목록과 규격은 `assets/img/NEEDED.md`에 있다. Codex가 생성해 같은 경로에 넣으면 자동으로 반영된다.

원칙:
- 사진보다 **일러스트/다이어그램**. 사내 업무 포털이고 여행 사이트가 아니다.
- 사람 사진을 쓰지 않는다. 특정 인물처럼 보이면 안 된다.
- SAP 로고·SK 로고를 **생성하지 않는다.** 실제 로고가 필요하면 공식 파일을 받아 쓴다.
- 파일당 200KB 이하, `.webp` 우선, `.png` 대체.

---

## 8. 접근성

- 본문 대비 **4.5:1 이상.** 노랑·연두 위 텍스트는 반드시 `-ink` 변형을 쓴다.
- 모듈 구분을 **색만으로 하지 않는다.** 색 + 모듈코드 텍스트 + 아이콘을 함께 쓴다.
- 모든 인터랙티브 요소에 `:focus-visible` 외곽선(SK Blue 2px).
- 다크모드는 만들지 않는다. 사내 포털이고 유지 비용만 늘어난다.
- 이미지 `alt`는 비워두지 않는다. 장식용이면 `alt=""`로 명시한다.

---

## 9. 구현 메모

- 프레임워크·빌드 없음. 순수 HTML + CSS 1개 + JS 1개.
- 외부 CDN 없음. 폰트·아이콘 전부 시스템/인라인 SVG.
- 모든 경로는 **상대경로**다. GitHub Pages 하위 경로에서도 그대로 동작한다.
- `build.mjs`는 HTML을 생성하는 도구다. 배포에는 필요 없다.
